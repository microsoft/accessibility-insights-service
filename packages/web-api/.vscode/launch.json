{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to web-api",
            "type": "node",
            "request": "attach",
            "preLaunchTask": "func-start",
            "resolveSourceMapLocations": ["${workspaceFolder}/../**/dist/**/*.js"]
        }
    ]
}
