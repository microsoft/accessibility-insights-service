{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to web-workers",
            "type": "node",
            "request": "attach",
            "preLaunchTask": "func-start",
            "resolveSourceMapLocations": ["${workspaceFolder}/../**/dist/**/*.js"]
        }
    ]
}
